<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:sharedUserId="android.uid.system">

    <!-- Lets the lock screen read active MediaSessions published by tools
         (e.g. via LightMediaSession). Signature|privileged permission, which
         the emulator legitimately holds as a platform-signed system app (uid
         1000); tools:ignore silences lint's system-app-only check. -->
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".EmulatorApplication"
        android:label="LightOS Emulator"
        android:supportsRtl="true"
        android:theme="@style/LightEmulator.Theme">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LightSdkPermissionActivity"
            android:exported="true"
            android:label="Light SDK Permission"
            android:theme="@style/LightEmulator.Theme.Permission" />
    </application>
</manifest>
